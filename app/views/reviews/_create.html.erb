<h2>CREATES</h2>
<table class='table'>
  <thead>
    <tr>
      <th>ID</th>
      <th>Created</th>
      <th>Author</th>
    </tr>
  </thead>

  <tbody>
<% versions.reverse.each do |v| %>
<tr>
    <td><%= link_to v.id, { action: 'show', id: v.id } %></td>
    <td><%= v.created_at %></td>
    <td><%= User.find(v.user_id).username %></td>
 </tr>
<% end %>
</tbody>
</table>
